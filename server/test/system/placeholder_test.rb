# frozen_string_literal: true

require 'application_system_test_case'

# This test is simply a placeholder until actual system tests are created.
# At least one "system" test is needed for SimpleCov to compile accurate
# code coverage statistics when running "rails test:system test"
class PlaceholderTest < ApplicationSystemTestCase
  test 'true' do
    assert true
  end
end
